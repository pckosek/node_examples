Javascritp showing the use of the 'fs' node module.

Read a file asynchronously and write it to the console.

This example offers immprovements that the name of text file can be passed to the function.